# 2026-05-14 外呼任务执行手册

任务已创建：`餐饮-2026-05-14-001`  
task_id：`1778676465986TH972IWOR7U94NO0Y`  
运行时段：09:00 – 20:00

---

## 阶段一：上午（任务运行前，任意时间）

### 1. 刷新迈鲸 session

打开迈鲸，用 agent-browser 执行登录，将 auth_context 写到文件：

```bash
# 或用现有 login workflow
python3 login/迈鲸登录-maijing-login/login_api.py --account-ref DXZG-1
```

假设 auth_context 输出到：
```
runs/2026-05-14/maijing-login-DXZG-1-001/outputs/maijing_auth_context.json
```

### 2. 重跑手机号拉取（获取 poi_code）

旧的 mobile_list 缺少 `poi_code`，需用 batch 002 重跑：

```bash
python3 workflows/迈鲸公海客户筛选导出-maijing-public-sea-filter-export/batch_regen_phones.py \
  --split-dir runs/2026-05-13/maijing-public-sea-export-execute-长沙市-002/outputs/split \
  --auth-context runs/2026-05-14/maijing-login-DXZG-1-001/outputs/maijing_auth_context.json \
  --batch 002
```

完成后 mobile_list 路径（含 poi_code）：
```
runs/2026-05-14/maijing-fetch-phone-by-id-餐饮-002/outputs/mobile_list_餐饮.json
```

### 3. 确认客户来源字段（可选）

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/import_leads_dry_run.py \
  --probe-history \
  --auth-context runs/2026-05-14/maijing-login-DXZG-1-001/outputs/maijing_auth_context.json
```

根据历史记录确认 `--customer-source` 的有效值（当前默认："AI外呼"）。

### 4. 生成导入 xlsx（dry-run 验证）

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/import_leads_dry_run.py \
  --mobile-list runs/2026-05-14/maijing-fetch-phone-by-id-餐饮-002/outputs/mobile_list_餐饮.json \
  --category 餐饮 \
  --customer-source AI外呼 \
  --batch 001
```

检查输出的 xlsx 和确认清单，确认无误后继续。

---

## 阶段二：20:00 之后（任务结束后）

### 5. 确认任务状态（agent-browser eval）

在火山控制台页面（已登录状态）执行：

```javascript
// 粘贴到 agent-browser eval
// 文件：workflows/火山外呼结果导出-volcengine-call-result-export/browser_eval/query_task.js
// TASK_ID 已设为：1778676465986TH972IWOR7U94NO0Y
```

确认 `Status: "FINISHED"` 后再继续。

### 6. 导出结果并获取下载链接（agent-browser eval）

```javascript
// 粘贴到 agent-browser eval
// 文件：workflows/火山外呼结果导出-volcengine-call-result-export/browser_eval/export_and_download.js
// TASK_ID 已设为：1778676465986TH972IWOR7U94NO0Y
// 脚本自动确认 FINISHED → 提交导出 → 轮询 → 返回 download_url
```

复制返回的完整 JSON，形如：
```json
{
  "task_id": "1778676465986TH972IWOR7U94NO0Y",
  "task_status": "FINISHED",
  "total_count": 196,
  "answer_count": ...,
  "export_id": "...",
  "download_url": "https://..."
}
```

### 7. 下载结果 CSV

将上面的 JSON 粘贴进命令：

```bash
python3 workflows/火山外呼结果导出-volcengine-call-result-export/receive_browser_result.py \
  --browser-result-json '{"task_id":"...","download_url":"...","answer_count":50,...}' \
  --category 餐饮 \
  --batch 001
```

输出：
- 结果 CSV：`runs/2026-05-14/volcengine-call-result-export-餐饮-001/outputs/result_餐饮.csv`
- 摘要：`result_summary.json`

### 7.5 检查 CSV 列结构（首次使用必做）

拿到 CSV 后先运行列结构检查，确认列名正确：

```bash
python3 common/Excel处理-excel-transform/inspect_csv.py \
  runs/2026-05-14/volcengine-call-result-export-餐饮-001/outputs/result_餐饮.csv
```

脚本会自动识别手机号列和状态列，并建议接通状态值。
若列名与预设不符，用 `--phone-col` / `--status-col` 参数覆盖（下一步使用）。

### 7.6 筛选接通客户 → 商机名单

```bash
python3 workflows/火山外呼结果导出-volcengine-call-result-export/parse_result_to_leads.py \
  --result-csv runs/2026-05-14/volcengine-call-result-export-餐饮-001/outputs/result_餐饮.csv \
  --mobile-list runs/2026-05-14/maijing-fetch-phone-by-id-餐饮-002/outputs/mobile_list_餐饮.json \
  --category 餐饮 \
  --batch 001
```

若 CSV 列名不符，追加：`--phone-col "实际列名" --status-col "实际列名" --answered-values "接通"`

输出：
- `runs/2026-05-14/volcengine-parse-result-餐饮-001/outputs/leads_for_import_餐饮.json`
- 接通客户列表（含 poi_code，可直接传给 import_leads_dry_run.py）

### 7.7 预览流程状态

```bash
python3 common/pipeline_status.py --date 2026-05-14
```

步骤 1-5 全部 OK 才能继续导入。

---

## 阶段三：导入迈鲸

### 8. 检查 Maijing session 是否仍有效

```bash
python3 login/迈鲸登录-maijing-login/check_session.py \
  --auth-context runs/2026-05-14/maijing-login-DXZG-1-001/outputs/maijing_auth_context.json
```

若 session 过期（❌），先重新登录再继续。上午登录后若超过 2 小时未操作可能过期。

### 9. 生成导入 xlsx（dry-run）

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/import_leads_dry_run.py \
  --mobile-list runs/2026-05-14/volcengine-parse-result-餐饮-001/outputs/leads_for_import_餐饮.json \
  --category 餐饮 \
  --customer-source AI外呼 \
  --batch 001
```

### 9.5 校验生成的 xlsx

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/validate_import_xlsx.py \
  --xlsx-file runs/2026-05-14/maijing-lead-import-餐饮-001/outputs/AI外呼商机导入_餐饮_001_20260514.xlsx \
  --expected-source AI外呼
```

全部 ✅ 再继续。若 ❌，检查 leads_for_import JSON 数据。

### 10. 生成人工确认 JSON

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/generate_confirmation.py \
  --run-dir runs/2026-05-14/maijing-lead-import-餐饮-001 \
  --category 餐饮 \
  --customer-source AI外呼 \
  --confirmed-by 你的姓名
```

脚本自动从 xlsx 统计行数并写入：
```
runs/2026-05-14/maijing-lead-import-餐饮-001/input/human_confirmation.json
```

若不传 `--confirmed-by`，脚本会写空值并提示手动填写。

### 11. 真实上传

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/import_leads_dry_run.py \
  --mobile-list runs/2026-05-14/volcengine-parse-result-餐饮-001/outputs/leads_for_import_餐饮.json \
  --category 餐饮 \
  --customer-source AI外呼 \
  --auth-context runs/2026-05-14/maijing-login-DXZG-1-001/outputs/maijing_auth_context.json \
  --confirmation-json runs/2026-05-14/maijing-lead-import-餐饮-001/input/human_confirmation.json \
  --execute-import \
  --batch 001
```

### 12. 验证并分析导入结果

```bash
# 查看最新导入历史
python3 workflows/迈鲸商机导入-maijing-lead-import/import_leads_dry_run.py \
  --probe-history \
  --auth-context runs/2026-05-14/maijing-login-DXZG-1-001/outputs/maijing_auth_context.json
```

上传成功后 import_history.json 在：
```
runs/2026-05-14/maijing-lead-import-餐饮-001/outputs/import_history.json
```

```bash
# 若有失败记录，分析失败原因
python3 workflows/迈鲸商机导入-maijing-lead-import/parse_import_failures.py \
  --history-json runs/2026-05-14/maijing-lead-import-餐饮-001/outputs/import_history.json \
  --leads-json runs/2026-05-14/volcengine-parse-result-餐饮-001/outputs/leads_for_import_餐饮.json
```

### 12.5 生成重试名单（若有 7天防重失败）

```bash
python3 workflows/迈鲸商机导入-maijing-lead-import/schedule_retry.py \
  --history-json runs/2026-05-14/maijing-lead-import-餐饮-001/outputs/import_history.json \
  --leads-json runs/2026-05-14/volcengine-parse-result-餐饮-001/outputs/leads_for_import_餐饮.json \
  --category 餐饮 \
  --import-date 2026-05-14
```

输出：`runs/2026-05-14/maijing-lead-import-餐饮-retry-20260521/outputs/retry_leads_餐饮.json`  
7天后（2026-05-21）将此文件作为 `--mobile-list` 重新执行步骤 9-11。

### 13. 生成批次汇总报告

```bash
python3 common/batch_report.py --date 2026-05-14 --category 餐饮
```

---

## 快速状态检查

```bash
# SOP 流程进度
python3 common/pipeline_status.py --date 2026-05-14

# 所有 run_dir 状态
python3 common/run_status_report.py --date 2026-05-14
```

---

## 注意事项

- 休闲娱乐品类（3 个移动号）本次跳过，不建议外呼。
- 若 mobile_list 没有 poi_code，所有记录会被跳过，上传条数为 0。
- 若导入失败原因为"7天内有大象跟进记录"，是业务防重机制，7天后可重试。
- 客户来源字段值"AI外呼"若被系统拒绝，用 `--probe-history` 查历史实际值，更新 `--customer-source`。
- 上午登录后若超过 2 小时再执行上传，先跑 `check_session.py` 确认 session 有效。
